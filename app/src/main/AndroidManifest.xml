<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="e.manan.test1">

    <!-- add internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- External storage permission -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Wallpaper permission -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Activities.Iman" />
        <activity android:name=".Activities.SocialMedia" />
        <activity android:name=".Activities.Community" />
        <activity android:name=".Activities.Donation" />
        <activity android:name=".Community.Register" />
        <activity android:name=".Community.MainCommActivity" />
        <activity android:name=".Community.ResetPasswordActivity" />
        <activity android:name=".Activities.PostsListActivity" />
        <activity android:name=".Activities.PostDetailActivity" />
        <activity android:name=".Activities.Chat_Room"></activity>
    </application>

</manifest>